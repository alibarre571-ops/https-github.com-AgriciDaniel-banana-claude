# Copy this file to .env for local development.
# On Render, these are set as environment variables in the dashboard
# (DATABASE_URL and PORT are provided automatically by the Blueprint).

DATABASE_URL=postgresql://wadani:wadani_dev_pw@localhost:5432/wadani_vgis
JWT_SECRET=replace-with-a-long-random-string
CORS_ORIGIN=*
PORT=4000
